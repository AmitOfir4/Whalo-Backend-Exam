version: '3.8'

services:
  # ── Infrastructure ──────────────────────────────────────────────
  redis:
    image: redis:7-alpine
    container_name: whalo-redis
    ports:
      - '6379:6379'
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - whalo-network

  rabbitmq:
    image: rabbitmq:3-management
    container_name: whalo-rabbitmq
    ports:
      - '5672:5672'
      - '15672:15672'
    environment:
      RABBITMQ_DEFAULT_USER: guest
      RABBITMQ_DEFAULT_PASS: guest
    healthcheck:
      test: ['CMD', 'rabbitmq-diagnostics', 'check_port_connectivity']
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - whalo-network

  # ── Microservices ───────────────────────────────────────────────
  player-service:
    build:
      context: .
      dockerfile: services/player-service/Dockerfile
    container_name: whalo-player-service
    ports:
      - '3001:3001'
      - '9234:9234'
    environment:
      MONGO_URI: ${MONGO_URI}
      PLAYER_SERVICE_PORT: 3001
      RABBITMQ_URL: amqp://guest:guest@rabbitmq:5672
    depends_on:
      rabbitmq:
        condition: service_healthy
    networks:
      - whalo-network
    restart: unless-stopped

  score-service:
    build:
      context: .
      dockerfile: services/score-service/Dockerfile
    container_name: whalo-score-service
    ports:
      - '3002:3002'
      - '9230:9230'
    environment:
      MONGO_URI: ${MONGO_URI}
      REDIS_URL: redis://redis:6379
      SCORE_SERVICE_PORT: 3002
      RABBITMQ_URL: amqp://guest:guest@rabbitmq:5672
    depends_on:
      redis:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    networks:
      - whalo-network
    restart: unless-stopped

  leaderboard-service:
    build:
      context: .
      dockerfile: services/leaderboard-service/Dockerfile
    container_name: whalo-leaderboard-service
    ports:
      - '3003:3003'
      - '9231:9231'
    environment:
      MONGO_URI: ${MONGO_URI}
      REDIS_URL: redis://redis:6379
      LEADERBOARD_SERVICE_PORT: 3003
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - whalo-network
    restart: unless-stopped

  log-service:
    build:
      context: .
      dockerfile: services/log-service/Dockerfile
    container_name: whalo-log-service
    ports:
      - '3004:3004'
      - '9232:9232'
    environment:
      RABBITMQ_URL: amqp://guest:guest@rabbitmq:5672
      LOG_SERVICE_PORT: 3004
    depends_on:
      rabbitmq:
        condition: service_healthy
    networks:
      - whalo-network
    restart: unless-stopped

  log-worker:
    build:
      context: .
      dockerfile: services/log-worker/Dockerfile
    container_name: whalo-log-worker
    ports:
      - '9233:9233'
    environment:
      MONGO_URI: ${MONGO_URI}
      RABBITMQ_URL: amqp://guest:guest@rabbitmq:5672
      BATCH_SIZE: 50
      BATCH_INTERVAL_MS: 2000
      MAX_CONCURRENT_WRITES: 3
      TOKEN_BUCKET_CAPACITY: 10
      TOKEN_BUCKET_REFILL_RATE: 5
    depends_on:
      rabbitmq:
        condition: service_healthy
    networks:
      - whalo-network
    restart: unless-stopped

  score-worker:
    build:
      context: .
      dockerfile: services/score-worker/Dockerfile
    container_name: whalo-score-worker
    ports:
      - '9235:9235'
    environment:
      MONGO_URI: ${MONGO_URI}
      REDIS_URL: redis://redis:6379
      RABBITMQ_URL: amqp://guest:guest@rabbitmq:5672
      BATCH_SIZE: 50
      BATCH_INTERVAL_MS: 2000
      MAX_CONCURRENT_WRITES: 3
      TOKEN_BUCKET_CAPACITY: 10
      TOKEN_BUCKET_REFILL_RATE: 5
    depends_on:
      redis:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    networks:
      - whalo-network
    restart: unless-stopped

networks:
  whalo-network:
    driver: bridge
